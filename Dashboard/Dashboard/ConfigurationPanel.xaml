<UserControl x:Class="Dashboard.ConfigurationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dashboard"
             mc:Ignorable="d" 
             d:DesignHeight="125" d:DesignWidth="300">
    <Grid Background="LightSlateGray">

        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Widget Title -->
        <TextBlock x:Name="Title" Text="Configuration Panel" FontSize="32" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Button x:Name="SnapButton" Grid.Row="1" Grid.Column="0" Margin="5" Content="Snap"/>

        <Button x:Name="ResizeButton" Grid.Row="1" Grid.Column="1" Margin="5" Content="Resize" Click="ResizeButton_Click"/>

        <Button x:Name="ExitButton" Grid.Row="1" Grid.Column="2" Margin="5" Content="Exit" Click="ExitButton_Click"/>

    </Grid>
</UserControl>
