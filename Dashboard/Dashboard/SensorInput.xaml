<UserControl x:Class="Dashboard.SensorInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dashboard"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="250">
    <Grid Background="LightSeaGreen" Margin="0,0,0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title Label -->
        <TextBlock Foreground="Black" Text="Sensor Input" FontSize="20" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontStyle="Oblique"/>


        <DataGrid Name="SensorInputGrid" ItemsSource="{Binding}" Margin="10,10,10,10" Grid.Row="1" Grid.RowSpan="4" Grid.ColumnSpan="2" AutoGeneratingColumn="SensorInputGrid_AutoGeneratingColumn" CanUserAddRows="False" HeadersVisibility="Column"/>
        
    </Grid>
</UserControl>
